{% extends 'CisDlsfBundle::layout.html.twig' %}

{% block title %}
    {{ parent() }} :: Edit Claim
{% endblock %}

{% block content %}

	{{ petroc_link(petroc_path('cis_dlsf.applicant.edit', {id: note.applicant.id}), 'Back to Edit') }}
	{{ form_start(form) }}
		<h2>Edit Note</h2>
		{{ form_row(form.notes) }}
	<div class="btn-group">
		{{ button('Save', {class:'primary'}) }}
	</div>
	{{ form_end(form) }}

{% endblock %}
